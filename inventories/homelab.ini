# Single Node k3s Cluster Configuration
# This is the default simple setup with one control plane + worker node

[servers]
10.0.0.175 ansible_user=davidg

[agents]
# Add worker-only nodes here if needed
# 10.0.0.176 ansible_user=davidg
# 10.0.0.177 ansible_user=davidg

# Group aliases for backward compatibility
[homelab:children]
servers
agents

# All nodes (servers + agents)
[all:children]
servers
agents

