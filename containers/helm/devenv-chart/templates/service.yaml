apiVersion: v1
kind: Service
metadata:
  name: {{ include "devenv.fullname" . }}
  labels:
    {{- include "devenv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: TCP
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "devenv.selectorLabels" . | nindent 4 }}